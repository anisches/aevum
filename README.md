# aevum

experimental agent. LLM as substrate, identity emerges from state.

each exchange increments a depth counter and runs skill detection — three gates that decide if something concrete happened, whether it generalizes, and whether it's been confirmed across multiple exchanges. skills that pass all three shape how the agent responds to the next question.

state lives at `~/.aevum/state.json`.
